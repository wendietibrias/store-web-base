import InputCn from "@/components/global/InputCn";
import { useForm } from "react-hook-form";

const Login = () => {
    const { register } = useForm();

    return (
        <div className="w-full">
         
        </div>
    )
}

export default Login;